# BL-019c — Wire document generation into deliverable store and UI download links

- Type: **task**
- Milestone: `M3-W2`
- Repo alignment: **missing**
- Labels: `backend`, `frontend`, `deliverables`
- Depends on: `BL-019b`, `BL-020`

## Problem

Output must be linked to runs and accessible from UI.

## Proposed solution

Add arq job to generate document, store artifact, emit deliverable_ready; UI shows download links and status.

## Acceptance criteria

- User can download generated document from UI; status updates via run events.

## Test plan

- E2E: run doc generation and download file.
