# BL-024 — DocuIntelli: mental model prompt library (templated reasoning patterns per framework)

- Type: **story**
- Milestone: `M7-DOCUINTELLI`
- Repo alignment: **missing**
- Labels: `domain:docuintelli`, `prompts`
- Depends on: `EPIC-DOCUINTELLI`, `BL-012`

## Problem

Different frameworks require different reasoning patterns and output schemas. Without a prompt library, behaviour is inconsistent and not auditable.

## Proposed solution

Create a mental-model prompt library:
- Templates per framework (dimensions, pattern matching, anomaly detection).
- Versioned prompts with tests.
- Output schemas enforced via validators (Pydantic + zod).

## Acceptance criteria

- At least 3 prompt templates exist and are versioned with tests.
- Prompt outputs validate against schemas reliably.

## Test plan

- Eval: prompt templates on fixtures produce valid outputs.
